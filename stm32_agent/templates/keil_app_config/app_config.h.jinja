#ifndef __APP_CONFIG_H
#define __APP_CONFIG_H

/*
 * 文件说明：
 * 1. 本文件由规划器自动生成，用于集中保存模块与引脚/外设实例的映射宏。
 * 2. 业务代码应优先引用这些宏，而不是直接写死 GPIO 口、引脚号或外设实例。
 * 3. 若重新规划工程，本文件会被覆盖，请勿在此手工维护业务逻辑。
 */
/* Auto-generated from planner assignments. */

{% for group in groups %}
/* {{ group.title }} */
{% for define in group.defines %}
#define {{ define.name }} {{ define.value }}
{% endfor %}
{% if not loop.last %}

{% endif %}
{% endfor %}
#endif
