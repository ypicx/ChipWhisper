#ifndef __MAIN_H
#define __MAIN_H

#ifdef __cplusplus
extern "C" {
#endif

/* 主头文件：集中声明工程公共入口并汇总 HAL/应用配置头文件。 */

#include "{{ main_hal_header }}"
{% for header in extra_headers %}
#include "{{ header }}"
{% endfor %}
#include "app_config.h"

/* 全局错误处理入口。遇到不可恢复错误时会进入该函数。 */
void Error_Handler(void);

#ifdef __cplusplus
}
#endif

#endif
